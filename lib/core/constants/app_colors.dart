import 'package:flutter/material.dart';

/// SwiftKeep design system - Indigo primary, Emerald accent, Slate text.
class AppColors {
  AppColors._();

  static const Color primary = Color(0xFF4F46E5); // Indigo Blue
  static const Color accent = Color(0xFF10B981);  // Emerald Green - Save, Add
  static const Color background = Color(0xFFF8FAFC); // Light Slate
  static const Color surface = Color(0xFFFFFFFF);   // Pure White
  static const Color textPrimary = Color(0xFF1E293B);   // Headings
  static const Color textSecondary = Color(0xFF64748B); // Secondary
  static const Color lowStock = Color(0xFFF59E0B);      // Amber
  static const Color error = Color(0xFFEF4444);
}
